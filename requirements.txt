# ── Core image processing ─────────────────────────────────────────────────────
opencv-python>=4.11.0
numpy>=1.26.4
Pillow>=10.2.0

# ── OCR (Arabic) ──────────────────────────────────────────────────────────────
paddleocr>=3.4.0
paddlepaddle>=3.2.0          # CPU build; for GPU use paddlepaddle-gpu instead

# ── Face recognition & liveness ───────────────────────────────────────────────
deepface>=0.0.99
# deepface pulls in tensorflow or torch automatically; torch is listed below
# for feature_pipeline.py optional deep-model support
torch>=2.2.2                 # CPU build; for GPU install the matching CUDA wheel

# ── Web UI (liveness_ui.py) ───────────────────────────────────────────────────
flask>=3.1.3

# ── Standard-library extras (usually included with Python, listed for clarity) -
# tkinter  — built into Python, no pip install needed
# argparse, json, pathlib, threading, re, logging, tempfile, shutil — stdlib
